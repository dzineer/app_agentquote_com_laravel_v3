import React, {Component} from 'react';
import PropTypes from 'prop-types';

/** class ServiceWorkerExample */
class ServiceWorkerExample extends Component {
    constructor(props) {
        super(props);
        this.state = {};
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}

ServiceWorkerExample.propTypes = {
    /** myProp */
    //myProp: PropTypes.string.isRequired
};

ServiceWorkerExample.defaultProps = {
    //myProp: val
};

export default ServiceWorkerExample;