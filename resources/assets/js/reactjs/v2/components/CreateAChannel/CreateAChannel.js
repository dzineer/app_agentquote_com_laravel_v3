import React, {Component} from 'react';
import PropTypes from 'prop-types';

/** class CreateAChannel */
class CreateAChannel extends Component {
    constructor(props) {
        super(props);
        this.state = {};
    }

    render() {
        return (
            <div>
adfds
            </div>
        );
    }
}

CreateAChannel.propTypes = {
    /** myProp */
    //myProp: PropTypes.string.isRequired
};

CreateAChannel.defaultProps = {
    //myProp: val
};

export default CreateAChannel;